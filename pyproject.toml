[project]
name = "smooth-lattice"
version = "0.1.0"
description = "Fixed-prime smooth-number rank/unrank experiments via recursive exponent lattices"
requires-python = ">=3.10"
dependencies = []

[tool.smooth-lattice]
status = "research-prototype"
