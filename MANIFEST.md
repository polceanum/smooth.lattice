# Repository manifest

This archive is intended to be dropped into:

```text
https://github.com/polceanum/smooth.lattice
```

Recommended first commit contents:

```text
README.md
AGENTS.md
build.sh
Makefile
smooth_toolkit.py
src/
benchmarks/
tests/
scripts/
docs/
paper/
results/
.github/workflows/ci.yml
```

Do not commit compiled binaries from `bin/`; they are ignored by `.gitignore`.
